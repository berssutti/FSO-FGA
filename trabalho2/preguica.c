#include <stdio.h>
#include <signal.h>

int main() {
    char signal;
    int i = 0;
    while(scanf("%s", signal) != EOF) {
        if (signal = "SIGUSR1")
        {
            printf("SIGUSR1 eh para aproveitar mais um pouco\n");
        } else if (signal = "SIGALRM") {
            if()
        }
        
    }
    
    return 0;
}